/**
 * Centralized image URL constants for all sections.
 * Uses static.photos education images.
 * Change URLs here to update images across the entire site.
 */

const BASE_URL = "https://static.photos/education/1024x576";

// ─── Hero Section (Slider) ───────────────────────────────────────
export const HERO_IMAGES = {
  slide1: `${BASE_URL}/10`,
  slide2: `${BASE_URL}/22`,
  slide3: `${BASE_URL}/35`,
};

// ─── Skill / Course Area ─────────────────────────────────────────
export const SKILL_COURSE_IMAGES = {
  ielts: `${BASE_URL}/42`,
  spokenEnglish: `${BASE_URL}/55`,
  ieltsLive: `${BASE_URL}/68`,
  juniorSpoken: `${BASE_URL}/80`,
};

// ─── Upcoming Events Banner ──────────────────────────────────────
export const EVENT_IMAGES = {
  event1: `${BASE_URL}/91`,
  event2: `${BASE_URL}/103`,
  event3: `${BASE_URL}/115`,
};

// ─── Portrait Videos (Marquee Carousel) ──────────────────────────
export const PORTRAIT_VIDEO_IMAGES = {
  video1: `${BASE_URL}/120`,
  video2: `${BASE_URL}/125`,
  video3: `${BASE_URL}/130`,
  video4: `${BASE_URL}/135`,
  video5: `${BASE_URL}/140`,
  video6: `${BASE_URL}/145`,
  video7: `${BASE_URL}/150`,
  video8: `${BASE_URL}/155`,
  video9: `${BASE_URL}/160`,
  video10: `${BASE_URL}/165`,
};

// ─── Video Player Section ────────────────────────────────────────
export const VIDEO_PLAYER_IMAGES = {
  thumbnail: `${BASE_URL}/170`,
};
